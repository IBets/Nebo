Запускать main
Управление осуществляется через W, A, S, D и движение мыши
Необходимые библиотеки:
    PyQt5
    PyOpenGL
    numpy



x_buffer -> Структуры для загрузки информации в них, затем на GPU
    buf_color.py
    buf_position.py
    buf_size.py

x_geometry -> Создержатся основные примитивы
    cube.py Cодежит атрибуты вершин куба(Координаты,
                                         Нормали,
                                         Текстурные координаты,
                                         Индексы вершин)


x_math -> Математическая библиотека
    angles.py           Углы Эйлера
    constant_tools.py   Константы и вспомогательные функции
    matrix.py           Матрицы порядка dim(4x4), dim(3x3)
    transform.py        Матрицы трасформации(Перемещение, поворот, смена базиса, матрицы перспективы)
    vector.py           Векторы размерности dim(3), dim(4)

x_render-> Файлы отвечающий за рендеринг
    ebo.py          Загружает индексный буффер на GPU
    glseprogram.py  Создает шейдрные программы и позволяет обращаться к uniform переменным
    texture.py      Отвечает за биндинг текстур
    vao.py          Связывает котексты буфферов для последующего прохода отрисовки
    vbo.py          Загружает буффер на GPU
    render.py       Скрывает интерфес OpenGL

x_shader
    basic.frag      Фрагментый шейдер Версия для Linux
    basic.vert      Вершинный шейдер  Версия для Linux
    basic_win.frag  Фрагментый шейдер Версия для Win
    basic_win.vert  Вершинный шейдер  Версия для Win

x_camera
    camera.py   Реализует камеру через углы эйлера

x_parser
    parser_stars.py  Парсит подваемую на вход строку с данными звезды

x_test -> Папка с файлами тестов
    test_math Тест математической библиотеки


x_widgets -> Виджеты Qt5
    widget_gl.py    Отвечает за рендер кадра, а также обработку системмы ввода(Мышь, Клавиатура)
    widget_input.py Виджет отвечаеющий за ввод данных пользователем


config.cfg config.py  Файлы конфигурации
main.py Файл запуска
run_test.py Файл для запуска тестов


